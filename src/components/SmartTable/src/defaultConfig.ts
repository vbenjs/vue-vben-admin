import type { SmartCheckboxConfig } from '@/components/SmartTable';

/**
 * 默认的checkbox配置
 */
export const defaultCheckboxConfig: SmartCheckboxConfig = {
  rowTrigger: 'single',
  rowCtrl: true,
  rowShift: true,
};
