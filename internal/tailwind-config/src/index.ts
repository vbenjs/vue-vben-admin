/**
 * TailwindCSS v4 configuration
 *
 * In v4, the theme configuration has moved to CSS (@theme directive).
 * See: packages/@core/base/design/src/css/global.css
 *
 * This package now provides:
 * - PostCSS config (./postcss)
 * - Iconify plugin wrapper (./plugins/iconify)
 */

export { default as iconifyPlugin } from './plugins/iconify';
