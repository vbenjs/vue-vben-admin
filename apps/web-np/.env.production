VITE_BASE=/

# 接口地址
VITE_GLOB_API_URL=https://np-api.widgetbuilder.app

# 是否开启压缩，可以设置为 none, brotli, gzip
VITE_COMPRESS=none

# 是否开启 PWA
VITE_PWA=false

# vue-router 的模式
VITE_ROUTER_HISTORY=web

# 是否注入全局loading
VITE_INJECT_APP_LOADING=true

# 打包后是否生成dist.zip
VITE_ARCHIVER=true

VITE_PUSHER_KEY=85c5f00e403477fedb45
VITE_PUSHER_CLUSTER=mt1
VITE_PUSHER_AUTH_ENDPOINT=https://np-api.widgetbuilder.app/broadcasting/auth
