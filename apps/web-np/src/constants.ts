export enum ShippingCostLevel {
  QUANTITY = 'QUANTITY',
  WEIGHT = 'WEIGHT',
}

export enum CostCalcLevel {
  PRODUCT = 'PRODUCT',
  VARIANT = 'VARIANT',
}

export enum DefaultRoutes {
  HOME = '/dashboard',
  ONBOARS = '/onboard',
}
